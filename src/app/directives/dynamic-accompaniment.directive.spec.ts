import { DynamicAccompanimentDirective } from './dynamic-accompaniment.directive';

describe('DynamicAccompanimentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicAccompanimentDirective();
    expect(directive).toBeTruthy();
  });
});
